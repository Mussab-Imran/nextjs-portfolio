/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['gracious-kare-44cbc0.netlify.app'], // Add your external image domain here
    },
  };
  
  export default nextConfig;
  